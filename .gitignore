# Python
venv/
__pycache__/
*.pyc

# Trained model (regenerated by dataset_stats.py)
model.json

# Cloned source projects (scanned to produce findings, never committed)
target_flask/
target_flask_config/
target_bandit/
target_vuln/
target_insecure_app/
target_werkzeug/

# Generated Bandit reports and worksheets (raw, regenerated on demand)
real_findings.json
bandit_examples.json
flask_config_findings.json
vuln_findings.json
insecure_app_findings.json
werkzeug_findings.json
constructed_findings.json
b301_constructed.json
findings_*.txt

# Constructed example source files (kept local for now, not committed yet)
constructed_examples/

# Backups
*.bak

# Held-out evaluation: cloned projects and raw/split scans (all regenerated)
target_django/
target_hackable/
target_keylogger/
target_pyload/
keylogger_findings.json
pyload_findings.json
django_b105_check.json
heldout_composition.py
django_findings.json
django_findings/
hackable_findings.json

# Editor/local
.claude/
docs/rule-coverage.md
# NOTE: intentionally NOT ignored (committed on purpose):
#   data/labeled_findings.json   -> training dataset
#   heldout/heldout_*.json       -> hand-labeled held-out test sets
#   b608_spec.txt                -> spec that rebuilds the B608 held-out
#   dataset_stats.md, misclassified.md, heldout_stats.md -> living reportsheldout_composition.py
